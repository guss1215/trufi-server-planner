# Dart
.dart_tool/
.packages
pubspec.lock
*.g.dart
build/

# IDE
.idea/
.vscode/
*.iml
*.code-workspace

# Flutter web build (generated artifact)
web/

# GTFS data — drop city-specific feeds under data/ (mounted at runtime).
# The committed example.gtfs.zip stays as a default for tests/dev.
data/

# Logs
*.log
